/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  images: {
    domains: ['img.clerk.com','i.postimg.cc'], // Add the Clerk image domain
  },
};

export default nextConfig;
